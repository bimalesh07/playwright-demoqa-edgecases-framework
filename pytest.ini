[pytest]
addopts = -v -s --headed --browser chromium

testpaths = 
    tests

 markers =
    ui: Frontend web automation tests with Playwright
    smoke: Critical sanity verification flows
    regression: Complete end-to-end framework test suite
